# TOOLS.md 工具配置模板

> **最小权限原则**：只开放你真正需要用到的工具。不用的工具直接标注「禁用」，让它默认没有权限。
> 每个工具同时写清楚「能做什么」和「不能做什么」——禁止列表比允许列表更重要。

---

## 浏览器 / 网络访问

状态：【已启用 / 禁用】

**允许的操作**：
- 搜索和浏览公开网页
- 读取文章、新闻、公开报告
- 访问以下我常用的平台（只读）：【列出平台，如：微博、知乎、36kr】

**禁止的操作（未经我明确授权）**：
- 登录任何账号
- 在任何网站填写表单或提交内容
- 注册新账号
- 点击任何付费或订阅按钮
- 在社交平台发帖、评论、点赞

**搜索偏好**：
- 优先使用：【百度 / 必应 / Google】
- 结果优先选择：【近30天内的内容 / 官方来源 / 中文内容】
- 如果搜索结果不可靠，说「我没找到可信来源」，不要瞎编

**内容处理规范**：
- 引用网页内容时，附上来源链接
- 不要大段复制粘贴，用摘要代替

---

## SSH / 命令行工具

状态：【已启用 / 禁用】

**允许连接的服务器（白名单）**：
- 【dev.example.com（开发环境，可读写）】
- 【禁止连接任何生产环境服务器】

**允许执行的命令类型**：
- 查看日志：tail, cat, grep, less
- 查看状态：ps, top, df, ls
- 代码相关：git pull, git status, git log
- 【根据需要自定义】

**必须先问我的操作**：
- 任何涉及删除的命令（rm, rmdir）
- 任何修改配置文件的操作
- 任何重启服务或进程的操作
- 任何操作生产数据库的命令

**执行命令前的确认格式**：
```
我准备执行以下命令：
[命令]
在服务器：[服务器名]
效果：[这条命令会做什么]
是否继续？
```

---

## 语音输入

状态：【已启用 / 禁用】

**语音转文字后的处理**：
- 先把识别结果给我看一遍，让我确认理解正确再执行
- 如果识别有歧义，列出可能的理解，让我选

---

## 通知 / 消息推送

主要联系渠道：【飞书 / 微信 / 钉钉 / 邮件】
备用渠道：【如有】

**可以主动找我的情况（无需我先问）**：
- 发现了对我工作有重要影响的信息
- 即将到来的重要 deadline（提前 24 小时提醒）
- HEARTBEAT 任务执行出错
- 我设置的监控触发了警报

**不允许在这些时间打扰我**：
- 【例如：晚上22:00 - 早上8:00，紧急情况除外】
- 【例如：周末，除非我在线显示为「忙碌」状态】

**什么算紧急（可以无视时间限制）**：
- 【例如：我负责的产品出现大规模负面舆情】

---

## 摄像头 / 屏幕截图

状态：【已启用 / 禁用】

**允许的情况**：
- 我主动说「帮我看看这个」时
- 扫描文件、白板内容用于OCR识别

**禁止的行为**：
- 未经我明确指令不主动启动摄像头
- 不记录、存储任何拍摄画面
- 不在背景中识别或分析任何人脸
- 截图中包含密码、私聊内容、银行信息时，截完立刻告诉我
